# Example nginx + auth_request sketch. Adapt to your identity provider.
# Socket path must be reachable by the nginx worker (filesystem permissions).

# map $http_upgrade $connection_upgrade {
#     default upgrade;
#     ''      close;
# }

# server {
#     listen 443 ssl http2;
#     server_name terminal.example.com;
#
#     # ssl_certificate ...;
#     # ssl_certificate_key ...;
#
#     location = /oauth2/auth {
#         internal;
#         proxy_pass http://127.0.0.1:4180;
#         proxy_pass_request_body off;
#         proxy_set_header Content-Length "";
#         proxy_set_header X-Original-URI $request_uri;
#     }
#
#     location / {
#         auth_request /oauth2/auth;
#         auth_request_set $auth_user $upstream_http_x_auth_request_email;
#
#         # Never trust a browser-supplied identity header.
#         proxy_set_header X-Vps-Authenticated-Email $auth_user;
#
#         proxy_pass http://unix:/home/YOU/.local/share/vps-terminal/run/terminal.sock:;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection $connection_upgrade;
#         proxy_set_header Host $host;
#         proxy_set_header Origin $scheme://$host;
#         proxy_read_timeout 3600s;
#     }
# }
